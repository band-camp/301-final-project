<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../../partials/head') %>
</head>
<body>
    <header>
        <%- include('../../partials/header') %>
    </header>

    <main>

        <div>
          <% evnetResults.forEach(event =>{ %>
            <ul id="bands">
              <li><button id= "<%= event.eventName %>" class="band-item"><a href="/bands/<%= event.eventName %>"><%= event.eventName %></a></button>
                
              </li>
                <ul id="events">
                  <li> </li>
                </ul>
            </ul>

          <% }) %>  
        </div>

    </main>

    <footer>
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
        <script> 
          $('.band-item').on('click', function() {
            console.log(this.id);
          });
        </script>
    </footer>
    
</body>
</html>